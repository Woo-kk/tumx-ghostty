# Claude Code Instructions

When a task is about operating `tmux-ghostty`, read `skills/tmux-ghostty/SKILL.md` first and follow it as the shared workflow.

Use `tmux-ghostty help` as the authoritative command reference.
